1/Common command
----------------

rm -r temp
mkdir temp
cp -r ARC/* temp/.
cd temp
for i in $(find_file_in_dir .gitignore | grep './')
do
	rm $i
done

2/GZIP Ramdisk
----------------

find . | cpio -o -H newc | gzip > ramdisk.cpio.gz


2/LZMA Ramdisk
----------------

*optional* sudo apt-get install lzip
find . | cpio -o -H newc | lzip > ramdisk.cpio.lz
